{
  "name": "scheduling-showcase",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An interactive CPU scheduling laboratory by cronfm",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/export-csharp.mjs",
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:parity": "node scripts/check-parity.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "prismjs": "1.30.0",
    "wrangler": "4.132.0"
  }
}
